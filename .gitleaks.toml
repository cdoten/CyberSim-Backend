title = "CyberSim gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Help docs with placeholder credential and secret examples"
paths = [
  '''(^|/)docs/airtable-handbook\.md$''',
  '''(^|/)docs/aws-elasticbeanstalk-deployment\.md$''',
]